STATIC INNER CLASS:
    1. Static members of outer class.
    2. Object of static inner class can be created outside of the outer class.
    3. No need to create an object of outer class.
    4. Static inner class can only access static members of the outer class.


    Outer.Inner i = new Outer.Inner();
    i.display.
